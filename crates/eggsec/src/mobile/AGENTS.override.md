# Mobile Module Override

## Key Files

| File | Purpose |
|------|---------|
| `crates/eggsec/src/mobile/mod.rs` | Module entry, public types (MobilePlatform, MobileFinding, MobileScanReport), run_cli dispatcher, format_mobile_report, to_scan_report_data bridge to unified reports; cfg-gated reexports + run_dynamic_cli for dynamic |
| `crates/eggsec/src/mobile/apk.rs` | Android APK static analysis (ZIP + AndroidManifest.xml binary/text AXML parsing, permissions, components, network-security-config, secrets, debug certs) |
| `crates/eggsec/src/mobile/ipa.rs` | iOS IPA static analysis (ZIP + Info.plist + embedded.mobileprovision + _CodeSignature, get-task-allow, provisioning profile risks) |
| `crates/eggsec/src/mobile/dynamic.rs` | Dynamic types (DynamicMobileReport/Finding, LabManifest, DynamicMobileArgs), run_dynamic_cli dispatcher, format_dynamic_report, to_scan_report_data_dynamic bridge |
| `crates/eggsec/src/mobile/adb.rs` | Pure-Rust ADB TCP framing (CNXN/OPEN/WRTE etc.) + AdbClient/AdbConnection (list, connect, shell, sync_push, install, launch, uninstall, capture_logcat); external `adb` only for discovery |
| `crates/eggsec/src/mobile/runtime.rs` | High-signal logcat parser (parse_logcat_findings): runtime-permission, crash-log, cleartext-observed, log-secret-leak (basic redaction) |
| `crates/eggsec/src/mobile/traffic.rs` | Phase 2 (closed 2026-06-12): TrafficSummary + parse_traffic_capture (summary-only parser for mitmproxy-style captures; domains/counts/cleartext/suspicious; feeds report + bridge info findings) |

## Implementation Notes

- **Phase 2 close-out complete 2026-06-12** (per `plans/mobile-dynamic-phase2-closeout-and-phase3-kickoff-plan.md`, executed): Phase 2 (proxy Level-1 + runtime permissions + correlation + hygiene) closed. All Phase 1+2 dynamic functionality kept under the single `mobile-dynamic` feature (M1 decision: no split to `mobile-dynamic-advanced`; decision recorded in Cargo.toml comments, docs/MOBILE.md, AGENTS.override.md, root AGENTS.md). Phase 3a (Frida foundation + basic_method_trace) delivered under single `mobile-dynamic` (per phase3-frida-expansion-plan.md Key Decision; no separate mobile-frida sub-feature; runtime --allow-frida + Intrusive policy gate for real ops; dry-run always safe). frida.rs now real (connect/execute_script/basic_method_trace + CLI fallback + simulation). Documentation updated for Phase 3a delivery. Stale "Phase 2a" references (prior name for the proxy/permissions/correlation work, now Phase 2 closed 2026-06-12) + prior mobile-frida language cleaned.
- **Pure-Rust only**: Uses `zip` crate (under `mobile` feature). No external binaries, no shelling out, no Frida/dynamic, no decompilation. Bounded extraction + size guards (200 MiB).
- **Phase 1 scope**: Static-only manifest/config surface (high-signal findings) + Phase 1 dynamic (Android ADB core + high-signal runtime logcat analysis) complete 2026-06-12. Lab/defense validation framing only. Explicit "authorized lab/defensive validation use only" note in CLI for both paths. Phase 1 polish (smoke test script `scripts/test-mobile-dynamic.sh`, `--list-devices` convenience, troubleshooting, docs + success criteria update) complete 2026-06-12 per `plans/mobile-dynamic-post-phase1-polish-and-phase2-planning.md` (executed).
- **Types**: `MobilePlatform` (Android/Ios), `MobileFinding` (category, Severity, title, description, recommendation, optional evidence), `MobileScanReport` (target, platform, app_id/version, findings, recommendations, duration). All serializable.
- **Bridge**: `to_scan_report_data()` converts to `crate::output::convert::ScanReportData` for JSON/SARIF/etc. consumers (pattern matches wireless module).
- **CLI surface**: Standalone `eggsec mobile <path.{apk,ipa}>` (legacy direct) or `eggsec mobile static ...` (static); `eggsec mobile dynamic ...` under `mobile-dynamic` feature. Handler uses `evaluate_and_enforce_operation` with `SafeActive` risk + feature gate (`mobile` for static, `mobile-dynamic` for dynamic). Dynamic adds DefenseLab mode + explicit `--allow-dynamic-mobile` gate for non-dry runs (audited). Not part of TUI tabs or pipeline profiles.
- **Enforcement**: `SafeActive` (low risk tier), no scope requirement, local file target only. Policy gate + descriptor in `commands/handlers/mobile.rs`. Dynamic descriptor: `DefenseLab + SafeActive + required_features: ["mobile-dynamic"]`. Dry-run always valid; real runs require explicit allow + best-effort cleanup (actions audited in report).

- **Dynamic (Phase 1 + Phase 2 closed 2026-06-12 + Phase 3a Frida foundation delivered under mobile-dynamic)**: Pure-Rust ADB TCP framing (emulator-XXXX primary; host:port supported); external `adb` binary only for `list_devices` convenience (plus `--list-devices` convenience per polish). High-signal-only log parser (no full log processing). Phase 2 (proxy foundation + runtime permissions + correlation) closed 2026-06-12 per combined closeout+kickoff plan (executed): new `traffic.rs`; adb helpers `set_global_proxy`/`clear_global_proxy` + `grant_permission`/`revoke_permission`/`list_permissions`; `DynamicMobileReport` extensions (`traffic_summary: Option<TrafficSummary>`, `permission_state: Option<PermissionState>`); `to_scan_report_data_dynamic` emits extra info findings (categories `mobile-dynamic-android-*`); `correlate_findings` populates `static_correlation`. Phase 3a (per phase3-frida-expansion-plan.md Key Decision, executed): Frida under single `mobile-dynamic` (no sub-feature); new `frida.rs` (real connect/execute_script + basic_method_trace builtin for Cipher/keystore/auth/detection methods + CLI fallback + dry simulation); CLI `--frida-script`/`--allow-frida`; handler policy (Intrusive for real Frida, SafeActive otherwise; explicit allow gate); report `frida_instrumentation` + frida-* findings + bridge categories (`mobile-dynamic-android-frida-*`). CLI flags: `--proxy`/`--reset-proxy`/`--traffic-capture` + `--grant-permission`/`--revoke-permission`/`--list-permissions` + `--frida-script`/`--allow-frida`. All under `mobile-dynamic` (M1 + Phase 3 Key Decision: no sub-feature split). Policy enforced in handler (DefenseLab mode + explicit `--allow-dynamic-mobile` for non-dry; `--allow-frida` for real Frida; audited). Dry-run always valid (produces complete serializable report, zero device/net side effects; Frida sim always safe). All actions recorded in `actions_performed`; best-effort cleanup (uninstall) always attempted on real install paths. Standalone defense-lab (MCP/agent absent; same pattern as wireless-active + static-mobile + auth-test). Final polish + close-out delivered correlation, parser robustness, report surface, doc hygiene. Phase 3a foundation + first builtin delivered. See `dynamic.rs:127`, `adb.rs`, `runtime.rs`, `traffic.rs`, `frida.rs`, `commands/handlers/mobile.rs:26`. Feature gating decision (M1 + Phase 3): all dynamic (incl. Frida Phase 3a) kept under the single `mobile-dynamic` feature (no split); decision recorded in Cargo.toml comments, docs/MOBILE.md, AGENTS.override.md, root AGENTS.md.

## Testing Guidance

- Unit tests use synthetic ZIP archives created in-memory (see `#[test]` blocks in `apk.rs` and `ipa.rs` using `zip::ZipWriter` + `Cursor`).
- Prefer small, deterministic synthetic fixtures for regression (manifest XML, binary AXML chunks, plists, provision profiles).
- No real APKs/IPAs or device tooling required for core parser tests.
- Run with feature:

```bash
cargo check -p eggsec --features mobile
cargo test --lib -p eggsec mobile::
cargo clippy --lib -p eggsec --features mobile

# Dynamic (P1)
cargo check -p eggsec --features mobile-dynamic
cargo test --lib -p eggsec --features mobile-dynamic
cargo clippy --lib -p eggsec --features mobile-dynamic
```

- Dynamic-specific tests: adb framing (duplex mocks in `adb.rs` tests), runtime parser on synthetic fixtures (`runtime.rs`), `Dynamic*` serde/bridge/dry-run (`dynamic.rs` tests + `to_scan_report_data_dynamic` categories), handler policy paths (static vs dynamic descriptors + allow gate). Phase 2 (closed): traffic summary parser tests (fixtures in `traffic.rs`), proxy/permission adb helpers (mocks), report field + bridge extensions (extra info findings for summary/state) + correlate_findings. Phase 3a (Frida under mobile-dynamic): frida.rs unit tests (dry connect/execute/trace, error paths when CLI missing, script gen markers, result parsing); dynamic.rs tests (dry frida actions/findings/carrier, to_scan bridge for frida-* + extra-info); smoke script Frida dry leg. All under --features mobile-dynamic (no mobile-frida). New tests cover frida dry paths + bridge categories (mobile-dynamic-android-frida-*). Still no real devices required for unit tests.

## Related

See `architecture/defense_lab.md`, wireless module (similar standalone defense-lab + bridge pattern; wireless active as direct precedent for dynamic mobile), `commands/handlers/mobile.rs`, `cli/mobile.rs`, and the mobile section in `AGENTS.md` (Key Types, Feature Flags, Security Notes). Dynamic loadout design in root `plans/dynamic-mobile-testing-loadout-design-plan.md`; Phase 1 implementation handoff in `plans/mobile-dynamic-phase1-implementation-handoff-plan.md` (executed 2026-06-12; mirrors wireless active precedent). Phase 1 polish (smoke test script `scripts/test-mobile-dynamic.sh`, `--list-devices` convenience, troubleshooting, docs + success criteria) complete 2026-06-12 per `plans/mobile-dynamic-post-phase1-polish-and-phase2-planning.md` (executed). Phase 2 (proxy + permissions + correlation) per `plans/mobile-dynamic-phase2-implementation-handoff-plan.md` + final polish + close-out per prior polish plans; Phase 2 officially closed 2026-06-12 per combined `plans/mobile-dynamic-phase2-closeout-and-phase3-kickoff-plan.md` (executed; all under single mobile-dynamic per M1 decision). Phase 3a (Frida foundation + basic_method_trace) delivered under mobile-dynamic per `plans/mobile-dynamic-phase3-frida-expansion-plan.md` (executed; Key Decision followed; frida.rs real + CLI/handler/report/bridge/smoke/docs). Testing commands (all under mobile-dynamic): cargo check/test/clippy -p eggsec --features mobile-dynamic; ./scripts/test-mobile-dynamic.sh (dry-run Frida leg included).

Phase 1 static closed 2026-06-11. Phase 1 dynamic (ADB + logcat) + polish complete 2026-06-12. Phase 2 closed 2026-06-12 (proxy/permissions/correlation/hygiene; all under mobile-dynamic; M1: no sub-feature split). Phase 3a (Frida foundation + basic_method_trace) delivered under single mobile-dynamic (Key Decision; runtime --allow-frida + policy; dry-run safe; frida.rs real). Final polish + close-out + Phase 3a complete 2026-06-12. Module stable for Phase 1 + Phase 2 + Phase 3a scope (static + dynamic + Frida; report surface, correlation, parser robustness, code hygiene, frida sim/real paths all in place). Future phases (3b/3c) per design plan.

Phase 2 close-out complete 2026-06-12 per `plans/mobile-dynamic-phase2-closeout-and-phase3-kickoff-plan.md` (executed). M1 decision documented: all Phase 1+2 dynamic functionality kept under the single mobile-dynamic feature (no split to mobile-dynamic-advanced); decision recorded in Cargo.toml comments, docs/MOBILE.md, AGENTS.override.md, root AGENTS.md. Phase 3a (Frida foundation) delivered under mobile-dynamic per phase3-frida-expansion-plan.md Key Decision (no separate mobile-frida; runtime --allow-frida + Intrusive gate; frida.rs now real implementation). Plan file annotated executed for 3a. Phase 4a (Core Correlation Engine + Evidence Foundation + polish handoff) delivered + executed 2026-06-12 per phase4-actionable-intelligence-plan.md + phase4a-final-polish-handoff-plan.md (non-breaking; CorrelationEngine + correlate_reports + enriched CorrelatedFinding (score/correlation_type/enrichment) + CorrelationResult (timeline + summary) + scoring in correlate_findings + build_timeline; builds on 3c MobileBaseline/--baseline/--evidence-bundle/regression; 6+ new tests + ~85 green; human reporting polish; docs + plans updated; TUI deferred per standalone; smoke covers baseline/evidence/multi-frida). All under single mobile-dynamic.
